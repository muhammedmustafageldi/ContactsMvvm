<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ContactsMvvm" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.ContactsMvvm" parent="Base.Theme.ContactsMvvm" />

    <!-- Custom Theme for specific Activity -->
    <style name="CustomStatusBarTheme" parent="Base.Theme.ContactsMvvm">
        <!-- Set the status bar color -->
        <item name="android:statusBarColor">@color/thirdColor</item>
        <!-- Optional: Make the status bar icons and text dark (for light status bar color) -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="EditText.OutlinedBox" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:minHeight">56dp</item>
        <item name="android:textSize">18sp</item>
        <item name="hintEnabled">true</item>
        <item name="android:textColor">@color/white</item>
        <item name="hintTextColor">@color/white</item>
        <item name="android:textColorHint">@color/white</item>
        <item name="boxStrokeColor">@color/white</item>
        <item name="boxStrokeWidth">2dp</item>
    </style>
</resources>